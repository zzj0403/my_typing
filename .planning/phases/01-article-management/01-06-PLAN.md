---
phase: 01-article-management
plan: 06
type: execute
wave: 3
depends_on: ['01-05']
files_modified:
  - src/components/ArticleList/index.tsx
  - src/components/ArticleList/index.module.less
  - src/components/ArticleList/types.ts
  - src/components/index.ts
autonomous: false
requirements: [ART-01, ART-05]
user_setup: []

must_haves:
  truths:
    - '用户可以看到所有文章列表（内置 + 上传）'
    - '用户可以通过分类筛选文章'
    - '用户可以点击选择一篇文章'
    - '用户可以上传 txt 文件添加新文章'
    - '用户可以删除已上传的文章'
    - '列表项有正确的选中状态高亮'
  artifacts:
    - path: 'src/components/ArticleList/index.tsx'
      provides: '文章列表 UI 组件'
      exports: ['ArticleList']
      min_lines: 150
    - path: 'src/components/ArticleList/index.module.less'
      provides: '文章列表样式'
      min_lines: 50
    - path: 'src/components/ArticleList/types.ts'
      provides: '组件类型定义'
      min_lines: 20
  key_links:
    - from: 'src/components/ArticleList/index.tsx'
      to: 'src/stores/articleStore.ts'
      via: 'useArticleStore hook'
      pattern: 'useArticleStore'
    - from: 'src/components/ArticleList/index.tsx'
      to: 'src/utils/fileParser.ts'
      via: 'parseTxtFile function'
      pattern: 'parseTxtFile'
---

<objective>
创建文章列表 UI 组件，实现文章展示、选择、上传和删除功能。

Purpose: 提供用户交互界面，完成文章选择 (ART-05) 和文件上传 (ART-01) 需求。Output: 文章列表组件 + 样式文件 </objective>

<execution_context> @/root/.claude/get-shit-done/workflows/execute-plan.md @/root/.claude/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-article-management/01-UI-SPEC.md
@.planning/phases/01-article-management/01-RESEARCH.md

# UI 规范摘要

## 布局结构

```
+------------------------------------------+
|  Header: 选择文章                          |
+------------------------------------------+
|  [上传文章 Button]                         |
+------------------------------------------+
|  Tabs: [全部] [诗词] [名言] [我的]          |
+------------------------------------------+
|  Article List                            |
|  +------------------------------------+  |
|  | 文章标题 1              [删除]     |  |
|  +------------------------------------+  |
|  | 文章标题 2                        |  |
|  +------------------------------------+  |
|  | [Empty state when no articles]          |
+------------------------------------------+
```

## 交互规范

- 文章列表项最小高度 44px
- 选中状态: 背景色 #e6f7ff，左侧 2px #1890ff 边框
- Hover 状态: 背景色 #f5f5f5
- 删除按钮仅对上传文章显示
- 文件上传限制: .txt 文件，最大 100KB

## 文案

- Primary CTA: 上传文章
- Empty state heading: 暂无上传文章
- Empty state body: 点击「上传文章」按钮导入 txt 文件，或选择下方内置诗词/名言开始练习
- Error state: 文件读取失败，请确保文件为 UTF-8 编码的 txt 格式
- Destructive confirmation: 删除文章：确认删除「{文章标题}」吗？此操作不可恢复 </context>

<interfaces>
<!-- 需要的 Store 和工具接口 -->
From src/stores/articleStore.ts:
```typescript
export interface ArticleState {
  articles: Article[]
  currentArticleId: string | null
  addArticle: (input: CreateArticleInput) => void
  removeArticle: (id: string) => void
  selectArticle: (id: string | null) => void
  getArticleById: (id: string) => Article | undefined
  getArticlesBySource: (source: ArticleSource) => Article[]
}

export const useArticleStore: () => ArticleState

````

From src/utils/fileParser.ts:
```typescript
export function parseTxtFile(file: File): Promise<string>
export function isValidTxtFile(file: File): boolean
export class FileParseError extends Error
````

From src/types/article.ts:

```typescript
export interface Article {
  id: string
  key: string
  title: string
  description?: string
  content: string
  sentences: Sentence[]
  source: 'builtin' | 'upload'
  createdAt: number
}

export type ArticleSource = 'builtin' | 'upload'
```

From Ant Design:

```typescript
import { List, Button, Upload, Tabs, Modal, Empty, message, Spin } from 'antd'
import {
  DeleteOutlined,
  FileTextOutlined,
  UploadOutlined,
} from '@ant-design/icons'
```

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 创建 ArticleList 组件类型定义</name>
  <files>src/components/ArticleList/types.ts</files>
  <read_first>
    - /root/my_typing/src/types/article.ts
  </read_first>
  <behavior>
    - Test 1: ArticleListProps 接口定义存在
    - Test 2: ArticleCategory 类型定义存在
  </behavior>
  <action>
    创建 src/components/ArticleList/types.ts:

    ```typescript
    import type { ArticleSource } from '@/types/article'

    /**
     * 文章分类
     */
    export type ArticleCategory = 'all' | 'poems' | 'quotes' | 'uploads'

    /**
     * 分类到来源的映射
     */
    export const CATEGORY_SOURCE_MAP: Record<ArticleCategory, ArticleSource | null> = {
      all: null,
      poems: 'builtin',
      quotes: 'builtin',
      uploads: 'upload',
    }

    /**
     * ArticleList 组件 Props
     */
    export interface ArticleListProps {
      /** 是否显示上传按钮 */
      showUpload?: boolean
      /** 是否显示分类 Tabs */
      showTabs?: boolean
      /** 初始选中的分类 */
      defaultCategory?: ArticleCategory
      /** 文章选中回调 */
      onSelect?: (articleId: string) => void
      /** 自定义类名 */
      className?: string
    }
    ```

  </action>
  <verify>
    <automated>grep -q "export type ArticleCategory" src/components/ArticleList/types.ts && grep -q "export interface ArticleListProps" src/components/ArticleList/types.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ArticleList/types.ts 存在
    - 文件导出 ArticleCategory 类型
    - 文件导出 CATEGORY_SOURCE_MAP 常量
    - 文件导出 ArticleListProps 接口
  </acceptance_criteria>
  <done>ArticleList 组件类型定义完成</done>
</task>

<task type="auto">
  <name>Task 2: 创建 ArticleList 样式文件</name>
  <files>src/components/ArticleList/index.module.less</files>
  <read_first>
    - /root/my_typing/.planning/phases/01-article-management/01-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: .articleList 类存在
    - Test 2: .articleItem 类存在
    - Test 3: .articleItemActive 类存在（选中状态）
    - Test 4: 选中状态包含左侧蓝色边框
  </behavior>
  <action>
    创建 src/components/ArticleList/index.module.less:

    ```less
    .articleList {
      display: flex;
      flex-direction: column;
      gap: 16px;
      padding: 16px;
    }

    .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 8px;
    }

    .title {
      font-size: 16px;
      font-weight: 600;
      color: #262626;
    }

    .tabs {
      margin-bottom: 16px;
    }

    .articleItem {
      display: flex;
      align-items: center;
      padding: 12px 16px;
      min-height: 44px;
      background: #ffffff;
      border-radius: 4px;
      cursor: pointer;
      transition: all 0.2s ease;
      border-left: 2px solid transparent;

      &:hover {
        background: #f5f5f5;
      }
    }

    .articleItemActive {
      background: #e6f7ff;
      border-left-color: #1890ff;

      &:hover {
        background: #e6f7ff;
      }
    }

    .articleIcon {
      margin-right: 12px;
      color: #8c8c8c;
      font-size: 16px;
    }

    .articleContent {
      flex: 1;
      min-width: 0;
    }

    .articleTitle {
      font-size: 14px;
      color: #262626;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }

    .articleDescription {
      font-size: 12px;
      color: #8c8c8c;
      margin-top: 4px;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }

    .deleteButton {
      opacity: 0;
      transition: opacity 0.2s ease;
      color: #ff4d4f;

      &:hover {
        color: #ff7875;
      }
    }

    .articleItem:hover .deleteButton {
      opacity: 1;
    }

    .emptyState {
      padding: 48px 16px;
      text-align: center;
    }

    .emptyTitle {
      font-size: 14px;
      color: #8c8c8c;
      margin-bottom: 8px;
    }

    .emptyDescription {
      font-size: 12px;
      color: #bfbfbf;
    }

    .loading {
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 48px;
    }

    .uploadArea {
      margin-bottom: 16px;
    }
    ```

  </action>
  <verify>
    <automated>grep -q ".articleList" src/components/ArticleList/index.module.less && grep -q ".articleItemActive" src/components/ArticleList/index.module.less</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ArticleList/index.module.less 存在
    - 文件包含 .articleList 类
    - 文件包含 .articleItem 类
    - 文件包含 .articleItemActive 类，且包含 border-left-color: #1890ff
    - 文件包含 .deleteButton 类
    - 文件包含 .emptyState 类
  </acceptance_criteria>
  <done>ArticleList 样式文件创建完成</done>
</task>

<task type="auto">
  <name>Task 3: 创建 ArticleList 主组件</name>
  <files>src/components/ArticleList/index.tsx</files>
  <read_first>
    - /root/my_typing/src/stores/articleStore.ts
    - /root/my_typing/src/utils/fileParser.ts
    - /root/my_typing/src/types/article.ts
    - /root/my_typing/src/components/ArticleList/types.ts
    - /root/my_typing/src/components/ArticleList/index.module.less
  </read_first>
  <behavior>
    - Test 1: 组件渲染文章列表
    - Test 2: 点击文章项触发 selectArticle
    - Test 3: 分类 Tabs 可切换过滤
    - Test 4: 上传按钮可触发文件选择
    - Test 5: 删除按钮可触发确认弹窗
  </behavior>
  <action>
    创建 src/components/ArticleList/index.tsx:

    ```typescript
    import React, { useState, useCallback, useMemo } from 'react'
    import { List, Button, Upload, Tabs, Modal, Empty, message, Spin } from 'antd'
    import { DeleteOutlined, FileTextOutlined, UploadOutlined } from '@ant-design/icons'

import { useArticleStore } from '@/stores/articleStore' import { parseTxtFile, isValidTxtFile, FileParseError } from '@/utils/fileParser' import type { Article, CreateArticleInput } from '@/types/article' import type { ArticleListProps, ArticleCategory } from './types' import { CATEGORY_SOURCE_MAP } from './types' import styles from './index.module.less'

    const { TabPane } = Tabs
    const { confirm } = Modal

    // 文件大小限制 100KB
    const MAX_FILE_SIZE = 100 * 1024

    /**
     * 文章列表组件
     * 展示所有文章，支持分类筛选、选择、上传和删除
     */
    const ArticleList: React.FC<ArticleListProps> = ({
      showUpload = true,
      showTabs = true,
      defaultCategory = 'all',
      onSelect,
      className,
    }) => {
      const [activeCategory, setActiveCategory] = useState<ArticleCategory>(defaultCategory)
      const [uploading, setUploading] = useState(false)

      const {
        articles,
        currentArticleId,
        addArticle,
        removeArticle,
        selectArticle,
      } = useArticleStore()

      // 根据分类过滤文章
      const filteredArticles = useMemo(() => {
        const source = CATEGORY_SOURCE_MAP[activeCategory]
        if (!source) {
          return articles
        }
        if (activeCategory === 'poems') {
          return articles.filter(a => a.source === 'builtin' && a.id.startsWith('poem-'))
        }
        if (activeCategory === 'quotes') {
          return articles.filter(a => a.source === 'builtin' && a.id.startsWith('quote-'))
        }
        return articles.filter(a => a.source === source)
      }, [articles, activeCategory])

      // 处理文章选择
      const handleSelect = useCallback((article: Article) => {
        selectArticle(article.id)
        onSelect?.(article.id)
      }, [selectArticle, onSelect])

      // 处理文件上传
      const handleUpload = useCallback(async (file: File) => {
        // 验证文件类型
        if (!isValidTxtFile(file)) {
          message.error('不支持的文件类型，请上传 .txt 文件')
          return false
        }

        // 验证文件大小
        if (file.size > MAX_FILE_SIZE) {
          message.error('文件大小超过 100KB 限制')
          return false
        }

        setUploading(true)
        try {
          const content = await parseTxtFile(file)
          const input: CreateArticleInput = {
            key: `upload-${Date.now()}`,
            title: file.name.replace(/\.txt$/i, ''),
            content,
            source: 'upload',
          }
          addArticle(input)
          message.success('文章上传成功')
          // 切换到"我的上传"分类
          setActiveCategory('uploads')
        } catch (error) {
          if (error instanceof FileParseError) {
            message.error(error.message)
          } else {
            message.error('文件读取失败，请确保文件为 UTF-8 编码的 txt 格式')
          }
        } finally {
          setUploading(false)
        }

        return false // 阻止默认上传行为
      }, [addArticle])

      // 处理文章删除
      const handleDelete = useCallback((article: Article, e: React.MouseEvent) => {
        e.stopPropagation()
        confirm({
          title: '删除文章',
          content: `确认删除「${article.title}」吗？此操作不可恢复`,
          okText: '删除',
          okType: 'danger',
          cancelText: '取消',
          onOk: () => {
            removeArticle(article.id)
            message.success('文章已删除')
          },
        })
      }, [removeArticle])

      // 渲染文章项
      const renderArticleItem = (article: Article) => {
        const isActive = article.id === currentArticleId
        const canDelete = article.source === 'upload'

        return (
          <div
            className={`${styles.articleItem} ${isActive ? styles.articleItemActive : ''}`}
            onClick={() => handleSelect(article)}
          >
            <FileTextOutlined className={styles.articleIcon} />
            <div className={styles.articleContent}>
              <div className={styles.articleTitle}>{article.title}</div>
              {article.description && (
                <div className={styles.articleDescription}>{article.description}</div>
              )}
            </div>
            {canDelete && (
              <Button
                type="text"
                icon={<DeleteOutlined />}
                className={styles.deleteButton}
                onClick={(e) => handleDelete(article, e)}
                aria-label="删除文章"
              />
            )}
          </div>
        )
      }

      // 渲染空状态
      const renderEmpty = () => {
        if (activeCategory === 'uploads') {
          return (
            <div className={styles.emptyState}>
              <Empty
                description={
                  <>
                    <div className={styles.emptyTitle}>暂无上传文章</div>
                    <div className={styles.emptyDescription}>
                      点击「上传文章」按钮导入 txt 文件，或选择下方内置诗词/名言开始练习
                    </div>
                  </>
                }
              />
            </div>
          )
        }
        return (
          <div className={styles.emptyState}>
            <Empty description="暂无文章" />
          </div>
        )
      }

      return (
        <div className={`${styles.articleList} ${className || ''}`}>
          {/* 上传按钮 */}
          {showUpload && (
            <div className={styles.uploadArea}>
              <Upload
                accept=".txt"
                showUploadList={false}
                beforeUpload={handleUpload}
              >
                <Button icon={<UploadOutlined />} loading={uploading}>
                  上传文章
                </Button>
              </Upload>
            </div>
          )}

          {/* 分类 Tabs */}
          {showTabs && (
            <Tabs
              activeKey={activeCategory}
              onChange={(key) => setActiveCategory(key as ArticleCategory)}
              className={styles.tabs}
            >
              <TabPane tab="全部" key="all" />
              <TabPane tab="诗词" key="poems" />
              <TabPane tab="名言" key="quotes" />
              <TabPane tab="我的" key="uploads" />
            </Tabs>
          )}

          {/* 文章列表 */}
          {uploading ? (
            <div className={styles.loading}>
              <Spin tip="上传中..." />
            </div>
          ) : filteredArticles.length > 0 ? (
            <div>
              {filteredArticles.map(article => (
                <div key={article.id}>
                  {renderArticleItem(article)}
                </div>
              ))}
            </div>
          ) : (
            renderEmpty()
          )}
        </div>
      )
    }

    export default ArticleList
    export type { ArticleListProps, ArticleCategory } from './types'
    ```

  </action>
  <verify>
    <automated>grep -q "export default ArticleList" src/components/ArticleList/index.tsx && grep -q "useArticleStore" src/components/ArticleList/index.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ArticleList/index.tsx 存在
    - 文件默认导出 ArticleList 组件
    - 组件使用 useArticleStore hook
    - 组件包含 Upload 组件处理文件上传
    - 组件包含 Tabs 组件处理分类切换
    - 组件包含 List 渲染文章列表
    - 组件包含 Modal.confirm 处理删除确认
    - 组件包含 message 处理操作反馈
    - 选中状态使用 .articleItemActive 样式类
    - 删除按钮仅对 source='upload' 的文章显示
    - 文件上传限制为 .txt 文件且最大 100KB
  </acceptance_criteria>
  <done>ArticleList 主组件创建完成</done>
</task>

<task type="auto">
  <name>Task 4: 更新组件导出</name>
  <files>src/components/index.ts</files>
  <read_first>
    - /root/my_typing/src/components/index.ts
    - /root/my_typing/src/components/ArticleList/index.tsx
  </read_first>
  <behavior>
    - Test 1: 从 @/components 可导入 ArticleList
  </behavior>
  <action>
    更新 src/components/index.ts，添加 ArticleList 导出:

    首先查看现有内容，然后添加:
    ```typescript
    // 现有导出保持不变
    // ...

    // 新增 ArticleList 导出
    export { default as ArticleList } from './ArticleList'
    export type { ArticleListProps, ArticleCategory } from './ArticleList/types'
    ```

  </action>
  <verify>
    <automated>grep -q "export.*ArticleList" src/components/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/components/index.ts 包含 ArticleList 的导出
    - 原有导出未被修改
  </acceptance_criteria>
  <done>组件导出更新完成</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    ArticleList 组件：文章列表 UI，包含分类 Tabs、文件上传、文章选择、删除功能
  </what-built>
  <how-to-verify>
    1. 启动开发服务器: pnpm dev
    2. 在应用中导航到文章列表页面（如需要，临时在 App.tsx 中添加 ArticleList 组件）
    3. 验证以下功能:
       - [ ] 文章列表正确显示所有内置文章
       - [ ] 分类 Tabs 切换正常（全部/诗词/名言/我的）
       - [ ] 点击文章项时选中状态高亮正确
       - [ ] 上传 .txt 文件成功后文章出现在列表中
       - [ ] 上传非 .txt 文件时显示错误提示
       - [ ] 删除上传文章时显示确认弹窗
       - [ ] 确认删除后文章从列表中移除
       - [ ] 刷新页面后上传的文章仍然存在（localStorage 持久化）
       - [ ] 刷新页面后内置文章重新加载
  </how-to-verify>
  <resume-signal>验证通过后输入 "approved"，如有问题请描述具体现象</resume-signal>
</task>

</tasks>

<verification>
- ArticleList 组件存在且功能完整
- 组件样式正确应用
- 文件上传功能正常
- 文章选择和删除功能正常
- 持久化功能正常
</verification>

<success_criteria>

- 用户可以看到分类过滤后的文章列表
- 用户可以上传 txt 文件添加新文章
- 用户可以选择一篇文章
- 用户可以删除已上传的文章
- UI 符合设计规范
- 刷新后数据正确恢复 </success_criteria>

<output>
完成后创建 `.planning/phases/01-article-management/01-06-SUMMARY.md`
</output>
