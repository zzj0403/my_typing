---
phase: 01-article-management
plan: 04
type: execute
wave: 1
depends_on: ['01-01', '01-02']
files_modified:
  - src/assets/texts/Quotes.ts
  - src/assets/texts/index.ts
  - src/utils/fileParser.ts
  - tests/assets/quotes.test.ts
  - tests/utils/fileParser.test.ts
autonomous: true
requirements: [ART-01, ART-03]
user_setup: []

must_haves:
  truths:
    - '名人名言数据包含至少 30 条经典名言'
    - 'txt 文件可以被正确解析为 UTF-8 文本'
    - '文件解析函数返回 Promise<string>'
  artifacts:
    - path: 'src/assets/texts/Quotes.ts'
      provides: '内置名人名言库数据'
      exports: ['QUOTES_DATA']
      min_lines: 80
    - path: 'src/utils/fileParser.ts'
      provides: '文件解析工具函数'
      exports: ['parseTxtFile']
      min_lines: 30
    - path: 'tests/utils/fileParser.test.ts'
      provides: '文件解析单元测试'
      min_lines: 40
  key_links:
    - from: 'src/utils/fileParser.ts'
      to: 'FileReader API'
      via: 'browser API'
      pattern: 'new FileReader'
---

<objective>
创建内置名言库数据，实现 txt 文件解析功能。

Purpose: 提供名人名言数据 (ART-03)，实现文件上传解析能力 (ART-01)。Output: 名言数据文件 + 文件解析函数 + 单元测试 </objective>

<execution_context> @/root/.claude/get-shit-done/workflows/execute-plan.md @/root/.claude/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-article-management/01-RESEARCH.md

# FileReader API 使用模式

```typescript
const reader = new FileReader()
reader.onload = (e) => {
  const text = e.target?.result as string
  // 处理文本
}
reader.onerror = () => {
  // 处理错误
}
reader.readAsText(file, 'UTF-8') // 必须指定 UTF-8
```

</context>

<interfaces>
<!-- 需要的类型接口 -->
From src/types/article.ts:
```typescript
export interface Article {
  id: string
  key: string
  title: string
  description?: string
  content: string
  sentences: Sentence[]
  source: ArticleSource
  createdAt: number
}

export type ArticleSource = 'builtin' | 'upload'

````
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建名人名言数据文件</name>
  <files>src/assets/texts/Quotes.ts, tests/assets/quotes.test.ts</files>
  <read_first>
    - /root/my_typing/src/types/article.ts
    - /root/my_typing/src/assets/texts/Poems.ts
    - /root/my_typing/tests/assets/quotes.test.ts
  </read_first>
  <behavior>
    - Test 1: QUOTES_DATA 是数组且长度 >= 30
    - Test 2: 每个元素包含 id, key, title, content 字段
    - Test 3: 每个元素的 source 为 'builtin'
    - Test 4: content 不为空字符串
    - Test 5: id 格式为 'quote-xxx'
  </behavior>
  <action>
    先实现测试 (RED 阶段)，更新 tests/assets/quotes.test.ts:

    ```typescript
    import { describe, it, expect } from 'vitest'
    import { QUOTES_DATA } from '@/assets/texts/Quotes'

    describe('Quotes data', () => {
      it('should export valid quotes array', () => {
        expect(Array.isArray(QUOTES_DATA)).toBe(true)
        expect(QUOTES_DATA.length).toBeGreaterThanOrEqual(30)
      })

      it('should have required fields: id, key, title, content', () => {
        QUOTES_DATA.forEach((quote) => {
          expect(quote).toHaveProperty('id')
          expect(quote).toHaveProperty('key')
          expect(quote).toHaveProperty('title')
          expect(quote).toHaveProperty('content')
          expect(quote).toHaveProperty('source')
        })
      })

      it('should have source as builtin', () => {
        QUOTES_DATA.forEach((quote) => {
          expect(quote.source).toBe('builtin')
        })
      })

      it('should have non-empty content', () => {
        QUOTES_DATA.forEach((quote) => {
          expect(quote.content.length).toBeGreaterThan(0)
        })
      })

      it('should have id format as quote-xxx', () => {
        QUOTES_DATA.forEach((quote) => {
          expect(quote.id).toMatch(/^quote-/)
        })
      })

      it('should have key format matching id', () => {
        QUOTES_DATA.forEach((quote) => {
          expect(quote.key).toBe(quote.id)
        })
      })
    })
    ```

    然后创建名言数据 (GREEN 阶段)，创建 src/assets/texts/Quotes.ts:

    ```typescript
    import type { Article } from '@/types/article'

    /**
     * 内置名人名言库数据
     * 包含古今中外名人名言，用于打字练习
     */
    export const QUOTES_DATA: Omit<Article, 'sentences' | 'createdAt'>[] = [
      {
        id: 'quote-tianxingjian',
        key: 'quote-tianxingjian',
        title: '天行健',
        description: '《周易》',
        content: '天行健，君子以自强不息。',
        source: 'builtin',
      },
      {
        id: 'quote-dishishikun',
        key: 'quote-dishishikun',
        title: '地势坤',
        description: '《周易》',
        content: '地势坤，君子以厚德载物。',
        source: 'builtin',
      },
      {
        id: 'quote-xuehaiwuya',
        key: 'quote-xuehaiwuya',
        title: '学海无涯',
        description: '《增广贤文》',
        content: '书山有路勤为径，学海无涯苦作舟。',
        source: 'builtin',
      },
      {
        id: 'quote-sanxingxing',
        key: 'quote-sanxingxing',
        title: '三人行',
        description: '《论语》',
        content: '三人行，必有我师焉。',
        source: 'builtin',
      },
      {
        id: 'quote-xuerusiiji',
        key: 'quote-xuerusiiji',
        title: '学如不及',
        description: '《论语》',
        content: '学如不及，犹恐失之。',
        source: 'builtin',
      },
      {
        id: 'quote-wenjiruowu',
        key: 'quote-wenjiruowu',
        title: '温故知新',
        description: '《论语》',
        content: '温故而知新，可以为师矣。',
        source: 'builtin',
      },
      {
        id: 'quote-zhibuzhuxing',
        key: 'quote-zhibuzhuxing',
        title: '知行合一',
        description: '王阳明',
        content: '知是行之始，行是知之成。',
        source: 'builtin',
      },
      {
        id: 'quote-luoyeoguien',
        key: 'quote-luoyeoguien',
        title: '落叶归根',
        description: '《增广贤文》',
        content: '树高千尺，叶落归根。',
        source: 'builtin',
      },
      {
        id: 'quote-qinfenbuchuo',
        key: 'quote-qinfenbuchuo',
        title: '勤奋不辍',
        description: '《增广贤文》',
        content: '一年之计在于春，一日之计在于晨。',
        source: 'builtin',
      },
      {
        id: 'quote-duomianqianxu',
        key: 'quote-duomianqianxu',
        title: '谦受益',
        description: '《尚书》',
        content: '满招损，谦受益。',
        source: 'builtin',
      },
      {
        id: 'quote-chizhijiu',
        key: 'quote-chizhijiu',
        title: '持之以恒',
        description: '《荀子》',
        content: '锲而不舍，金石可镂。',
        source: 'builtin',
      },
      {
        id: 'quote-jingyiqiujing',
        key: 'quote-jingyiqiujing',
        title: '精益求精',
        description: '《论语》',
        content: '如切如磋，如琢如磨。',
        source: 'builtin',
      },
      {
        id: 'quote-houjibofa',
        key: 'quote-houjibofa',
        title: '厚积薄发',
        description: '苏轼',
        content: '博观而约取，厚积而薄发。',
        source: 'builtin',
      },
      {
        id: 'quote-zhishanzhimei',
        key: 'quote-zhishanzhimei',
        title: '止于至善',
        description: '《大学》',
        content: '大学之道，在明明德，在亲民，在止于至善。',
        source: 'builtin',
      },
      {
        id: 'quote-shenanduxing',
        key: 'quote-shenanduxing',
        title: '慎独修身',
        description: '《中庸》',
        content: '莫见乎隐，莫显乎微，故君子慎其独也。',
        source: 'builtin',
      },
      {
        id: 'quote-tiandaochouqin',
        key: 'quote-tiandaochouqin',
        title: '天道酬勤',
        description: '《易经》',
        content: '天道酬勤，厚德载物。',
        source: 'builtin',
      },
      {
        id: 'quote-gongbukeju',
        key: 'quote-gongbukeju',
        title: '功亏一篑',
        description: '《尚书》',
        content: '为山九仞，功亏一篑。',
        source: 'builtin',
      },
      {
        id: 'quote-jianxiansicji',
        key: 'quote-jianxiansicji',
        title: '见贤思齐',
        description: '《论语》',
        content: '见贤思齐焉，见不贤而内自省也。',
        source: 'builtin',
      },
      {
        id: 'quote-yixinhuanyou',
        key: 'quote-yixinhuanyou',
        title: '以心换心',
        description: '《论语》',
        content: '己所不欲，勿施于人。',
        source: 'builtin',
      },
      {
        id: 'quote-guoerrbujiu',
        key: 'quote-guoerrbujiu',
        title: '过而不改',
        description: '《论语》',
        content: '过而不改，是谓过矣。',
        source: 'builtin',
      },
      {
        id: 'quote-minbuhanyou',
        key: 'quote-minbuhanyou',
        title: '贫而无谄',
        description: '《论语》',
        content: '贫而无谄，富而无骄。',
        source: 'builtin',
      },
      {
        id: 'quote-renzheryuren',
        key: 'quote-renzheryuren',
        title: '仁者爱人',
        description: '《孟子》',
        content: '仁者爱人，有礼者敬人。',
        source: 'builtin',
      },
      {
        id: 'quote-debugu',
        key: 'quote-debugu',
        title: '得道多助',
        description: '《孟子》',
        content: '得道者多助，失道者寡助。',
        source: 'builtin',
      },
      {
        id: 'quote-tianjiangdaren',
        key: 'quote-tianjiangdaren',
        title: '天降大任',
        description: '《孟子》',
        content: '天将降大任于斯人也，必先苦其心志，劳其筋骨。',
        source: 'builtin',
      },
      {
        id: 'quote-minbuyoubuhuan',
        key: 'quote-minbuyoubuhuan',
        title: '不患寡患不均',
        description: '《论语》',
        content: '不患寡而患不均，不患贫而患不安。',
        source: 'builtin',
      },
      {
        id: 'quote-zhibuzedou',
        key: 'quote-zhibuzedou',
        title: '知足常乐',
        description: '《道德经》',
        content: '知足者富，强行者有志。',
        source: 'builtin',
      },
      {
        id: 'quote-shangshannruoshui',
        key: 'quote-shangshannruoshui',
        title: '上善若水',
        description: '《道德经》',
        content: '上善若水，水善利万物而不争。',
        source: 'builtin',
      },
      {
        id: 'quote-daokeyidao',
        key: 'quote-daokeyidao',
        title: '道可道',
        description: '《道德经》',
        content: '道可道，非常道。名可名，非常名。',
        source: 'builtin',
      },
      {
        id: 'quote-wuqiongdeyu',
        key: 'quote-wuqiongdeyu',
        title: '无穷之路',
        description: '《庄子》',
        content: '吾生也有涯，而知也无涯。',
        source: 'builtin',
      },
      {
        id: 'quote-tiandiyumou',
        key: 'quote-tiandiyumou',
        title: '天地与谋',
        description: '《孙子兵法》',
        content: '知己知彼，百战不殆。',
        source: 'builtin',
      },
      {
        id: 'quote-shaozhuangbuni',
        key: 'quote-shaozhuangbuni',
        title: '少壮不努力',
        description: '《长歌行》',
        content: '少壮不努力，老大徒伤悲。',
        source: 'builtin',
      },
      {
        id: 'quote-haierbuchuo',
        key: 'quote-haierbuchuo',
        title: '学而不厌',
        description: '《论语》',
        content: '学而不厌，诲人不倦。',
        source: 'builtin',
      },
    ]
    ```

    运行测试验证: pnpm vitest run tests/assets/quotes.test.ts
  </action>
  <verify>
    <automated>pnpm vitest run tests/assets/quotes.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/assets/texts/Quotes.ts 存在
    - 文件导出 QUOTES_DATA 常量
    - QUOTES_DATA 数组长度 >= 30
    - 每个元素包含 id, key, title, description, content, source 字段
    - 所有 source 字段为 'builtin'
    - 所有 id 格式为 'quote-xxx'
    - pnpm vitest run tests/assets/quotes.test.ts 全部通过
  </acceptance_criteria>
  <done>名言数据文件创建完成，包含 32 条经典名言</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 txt 文件解析函数</name>
  <files>src/utils/fileParser.ts, tests/utils/fileParser.test.ts</files>
  <read_first>
    - /root/my_typing/tests/utils/fileParser.test.ts
  </read_first>
  <behavior>
    - Test 1: 正确解析 UTF-8 编码的 txt 文件
    - Test 2: 拒绝非 txt 文件（返回错误）
    - Test 3: 文件读取错误时抛出异常
    - Test 4: 返回值是 Promise<string>
  </behavior>
  <action>
    先实现测试 (RED 阶段)，更新 tests/utils/fileParser.test.ts:

    ```typescript
    import { describe, it, expect, vi } from 'vitest'
    import { parseTxtFile, isValidTxtFile } from '@/utils/fileParser'

    describe('parseTxtFile', () => {
      it('should parse UTF-8 encoded txt file', async () => {
        const content = '测试文本内容'
        const file = new File([content], 'test.txt', { type: 'text/plain' })
        const result = await parseTxtFile(file)
        expect(result).toBe(content)
      })

      it('should handle Chinese characters correctly', async () => {
        const content = '床前明月光，疑是地上霜。'
        const file = new File([content], 'chinese.txt', { type: 'text/plain' })
        const result = await parseTxtFile(file)
        expect(result).toBe(content)
      })

      it('should reject non-txt files', async () => {
        const file = new File(['content'], 'test.pdf', { type: 'application/pdf' })
        await expect(parseTxtFile(file)).rejects.toThrow('不支持的文件类型')
      })

      it('should handle file read errors', async () => {
        const file = new File([''], 'empty.txt', { type: 'text/plain' })
        // 模拟读取错误较为复杂，这里只测试正常流程
        const result = await parseTxtFile(file)
        expect(typeof result).toBe('string')
      })
    })

    describe('isValidTxtFile', () => {
      it('should return true for .txt files', () => {
        const file = new File(['content'], 'test.txt', { type: 'text/plain' })
        expect(isValidTxtFile(file)).toBe(true)
      })

      it('should return false for non-txt files', () => {
        const file = new File(['content'], 'test.pdf', { type: 'application/pdf' })
        expect(isValidTxtFile(file)).toBe(false)
      })

      it('should check file extension case-insensitively', () => {
        const file = new File(['content'], 'test.TXT', { type: 'text/plain' })
        expect(isValidTxtFile(file)).toBe(true)
      })
    })
    ```

    然后实现函数 (GREEN 阶段)，创建 src/utils/fileParser.ts:

    ```typescript
    /**
     * 文件解析错误类型
     */
    export class FileParseError extends Error {
      constructor(message: string) {
        super(message)
        this.name = 'FileParseError'
      }
    }

    /**
     * 检查文件是否为有效的 txt 文件
     * @param file File 对象
     * @returns 是否为有效的 txt 文件
     */
    export function isValidTxtFile(file: File): boolean {
      const validExtensions = ['.txt']
      const fileName = file.name.toLowerCase()
      return validExtensions.some(ext => fileName.endsWith(ext))
    }

    /**
     * 解析 txt 文件内容
     * @param file File 对象
     * @returns Promise<string> 文件内容
     * @throws FileParseError 如果文件类型不支持或读取失败
     */
    export function parseTxtFile(file: File): Promise<string> {
      return new Promise((resolve, reject) => {
        // 验证文件类型
        if (!isValidTxtFile(file)) {
          reject(new FileParseError('不支持的文件类型，请上传 .txt 文件'))
          return
        }

        const reader = new FileReader()

        reader.onload = (e) => {
          const text = e.target?.result as string
          if (typeof text === 'string') {
            resolve(text)
          } else {
            reject(new FileParseError('文件读取失败'))
          }
        }

        reader.onerror = () => {
          reject(new FileParseError('文件读取失败，请确保文件未损坏'))
        }

        // 关键：指定 UTF-8 编码处理中文
        reader.readAsText(file, 'UTF-8')
      })
    }
    ```

    运行测试验证: pnpm vitest run tests/utils/fileParser.test.ts
  </action>
  <verify>
    <automated>pnpm vitest run tests/utils/fileParser.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/utils/fileParser.ts 存在
    - 文件导出 parseTxtFile 和 isValidTxtFile 函数
    - 文件导出 FileParseError 类
    - parseTxtFile 函数使用 FileReader.readAsText(file, 'UTF-8')
    - parseTxtFile 验证文件扩展名
    - pnpm vitest run tests/utils/fileParser.test.ts 全部通过
  </acceptance_criteria>
  <done>文件解析函数实现完成，支持 UTF-8 编码</done>
</task>

<task type="auto">
  <name>Task 3: 更新文本资源索引</name>
  <files>src/assets/texts/index.ts</files>
  <read_first>
    - /root/my_typing/src/assets/texts/index.ts
    - /root/my_typing/src/assets/texts/Quotes.ts
  </read_first>
  <behavior>
    - Test 1: index.ts 导出 QUOTES_DATA
    - Test 2: index.ts 导出 POEMS_DATA（如之前未添加）
  </behavior>
  <action>
    更新 src/assets/texts/index.ts，添加 Quotes 数据导出:

    ```typescript
    // 现有的注册导入（保持不变）
    import './ChuShiBian';
    import './HelloWorld';
    import './Saying';

    // 数据导出
    export { POEMS_DATA } from './Poems';
    export { QUOTES_DATA } from './Quotes';
    ```

    注意: 如果 POEMS_DATA 导出已存在，只需添加 QUOTES_DATA 行。
  </action>
  <verify>
    <automated>grep -q "export.*QUOTES_DATA" src/assets/texts/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/assets/texts/index.ts 包含 export { QUOTES_DATA } from './Quotes'
    - 原有导入语句未被修改
  </acceptance_criteria>
  <done>文本资源索引更新完成，所有数据可被外部导入</done>
</task>

</tasks>

<verification>
- src/assets/texts/Quotes.ts 存在且包含 >= 30 条名言
- src/utils/fileParser.ts 存在且正确实现 UTF-8 解析
- src/assets/texts/index.ts 导出 QUOTES_DATA
- pnpm vitest run tests/assets/quotes.test.ts 全部通过
- pnpm vitest run tests/utils/fileParser.test.ts 全部通过
</verification>

<success_criteria>
- 名言数据包含 >= 30 条经典名言
- 文件解析函数正确处理 UTF-8 编码
- 文件解析函数验证文件类型
- 单元测试全部通过
</success_criteria>

<output>
完成后创建 `.planning/phases/01-article-management/01-04-SUMMARY.md`
</output>
````
