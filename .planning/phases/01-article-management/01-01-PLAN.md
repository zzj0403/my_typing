---
phase: 01-article-management
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - vitest.config.ts
  - tests/utils/sentenceSplit.test.ts
  - tests/utils/fileParser.test.ts
  - tests/stores/articleStore.test.ts
  - tests/assets/poems.test.ts
  - tests/assets/quotes.test.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - 'Vitest 测试框架已安装并配置'
    - '所有测试文件存在并可运行'
  artifacts:
    - path: 'vitest.config.ts'
      provides: 'Vitest 测试配置'
      exports: ['defineConfig']
    - path: 'tests/utils/sentenceSplit.test.ts'
      provides: '分句函数测试骨架'
      min_lines: 10
    - path: 'tests/utils/fileParser.test.ts'
      provides: '文件解析测试骨架'
      min_lines: 10
    - path: 'tests/stores/articleStore.test.ts'
      provides: 'Store 测试骨架'
      min_lines: 10
    - path: 'tests/assets/poems.test.ts'
      provides: '诗词数据验证测试骨架'
      min_lines: 10
    - path: 'tests/assets/quotes.test.ts'
      provides: '名言数据验证测试骨架'
      min_lines: 10
  key_links:
    - from: 'vitest.config.ts'
      to: 'package.json'
      via: 'vitest dependency'
      pattern: 'vitest'
---

<objective>
搭建测试基础设施，为后续任务提供验证能力。

Purpose: 所有后续功能开发都需要自动化测试验证，Wave 0 先创建测试框架和测试文件骨架。Output: 可运行的 Vitest 测试框架 + 所有测试文件骨架 </objective>

<execution_context> @/root/.claude/get-shit-done/workflows/execute-plan.md @/root/.claude/get-shit-done/templates/summary.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-article-management/01-VALIDATION.md

# 现有 package.json scripts

```json
{
  "scripts": {
    "dev": "cross-env BROWSER=chrome vite",
    "build": "tsc && vite build",
    "serve": "vite preview",
    "start": "npm run dev"
  }
}
```

</context>

<tasks>

<task type="auto">
  <name>Task 1: 安装 Vitest 测试框架</name>
  <files>package.json</files>
  <read_first>
    - /root/my_typing/package.json
  </read_first>
  <behavior>
    - Test 1: pnpm vitest run 命令可以执行
    - Test 2: package.json devDependencies 包含 vitest
  </behavior>
  <action>
    1. 安装 Vitest 及相关测试依赖:
       ```bash
       pnpm add -D vitest @testing-library/react @testing-library/jest-dom
       ```
    2. 在 package.json 中添加测试脚本:
       - "test": "vitest run"
       - "test:watch": "vitest"
    3. 验证安装成功: pnpm vitest --version
  </action>
  <verify>
    <automated>grep -q '"vitest"' package.json && pnpm vitest --version</automated>
  </verify>
  <acceptance_criteria>
    - package.json devDependencies 包含 "vitest"
    - package.json scripts 包含 "test": "vitest run"
    - pnpm vitest --version 命令成功执行
  </acceptance_criteria>
  <done>Vitest 框架安装完成，test 脚本可运行</done>
</task>

<task type="auto">
  <name>Task 2: 创建 Vitest 配置文件</name>
  <files>vitest.config.ts</files>
  <read_first>
    - /root/my_typing/package.json
    - /root/my_typing/tsconfig.json
  </read_first>
  <behavior>
    - Test 1: vitest.config.ts 文件存在
    - Test 2: pnpm vitest run 不报配置错误
  </behavior>
  <action>
    创建 vitest.config.ts 文件，内容如下:
    ```typescript
    import { defineConfig } from 'vitest/config'
    import react from '@vitejs/plugin-react'
    import path from 'path'

    export default defineConfig({
      plugins: [react()],
      test: {
        globals: true,
        environment: 'jsdom',
        include: ['tests/**/*.test.ts', 'tests/**/*.test.tsx'],
        exclude: ['node_modules', 'src/.vit'],
        coverage: {
          reporter: ['text', 'json', 'html'],
          exclude: ['node_modules', 'tests/']
        }
      },
      resolve: {
        alias: {
          '@': path.resolve(__dirname, './src')
        }
      }
    })
    ```

    如果缺少 jsdom 依赖，执行: pnpm add -D jsdom

  </action>
  <verify>
    <automated>test -f vitest.config.ts && grep -q "defineConfig" vitest.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - vitest.config.ts 文件存在
    - 文件包含 "defineConfig" 导入和使用
    - 文件包含 environment: 'jsdom' 配置
    - 文件包含 @ 路径别名配置
  </acceptance_criteria>
  <done>Vitest 配置文件创建完成，支持 React 组件测试</done>
</task>

<task type="auto">
  <name>Task 3: 创建测试文件骨架</name>
  <files>tests/utils/sentenceSplit.test.ts, tests/utils/fileParser.test.ts, tests/stores/articleStore.test.ts, tests/assets/poems.test.ts, tests/assets/quotes.test.ts</files>
  <read_first>
    - /root/my_typing/vitest.config.ts
  </read_first>
  <behavior>
    - Test 1: 所有测试文件存在
    - Test 2: pnpm test 运行无错误（测试通过或跳过）
  </behavior>
  <action>
    创建以下测试文件骨架，每个文件包含 describe 块和一个占位测试:

    1. tests/utils/sentenceSplit.test.ts:
    ```typescript
    import { describe, it, expect } from 'vitest'

    describe('splitIntoSentences', () => {
      it.todo('should split text by Chinese punctuation')
      it.todo('should preserve punctuation at end of sentences')
      it.todo('should handle empty string')
      it.todo('should handle text without punctuation')
    })
    ```

    2. tests/utils/fileParser.test.ts:
    ```typescript
    import { describe, it, expect } from 'vitest'

    describe('parseTxtFile', () => {
      it.todo('should parse UTF-8 encoded txt file')
      it.todo('should reject non-txt files')
      it.todo('should handle file read errors')
    })
    ```

    3. tests/stores/articleStore.test.ts:
    ```typescript
    import { describe, it, expect, beforeEach } from 'vitest'

    describe('useArticleStore', () => {
      it.todo('should add article')
      it.todo('should remove article')
      it.todo('should select article')
      it.todo('should persist uploaded articles to localStorage')
    })
    ```

    4. tests/assets/poems.test.ts:
    ```typescript
    import { describe, it, expect } from 'vitest'

    describe('Poems data', () => {
      it.todo('should export valid poem array')
      it.todo('should have required fields: id, title, content')
    })
    ```

    5. tests/assets/quotes.test.ts:
    ```typescript
    import { describe, it, expect } from 'vitest'

    describe('Quotes data', () => {
      it.todo('should export valid quotes array')
      it.todo('should have required fields: id, title, content')
    })
    ```

  </action>
  <verify>
    <automated>pnpm test 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/utils/sentenceSplit.test.ts 存在且包含 describe('splitIntoSentences')
    - tests/utils/fileParser.test.ts 存在且包含 describe('parseTxtFile')
    - tests/stores/articleStore.test.ts 存在且包含 describe('useArticleStore')
    - tests/assets/poems.test.ts 存在且包含 describe('Poems data')
    - tests/assets/quotes.test.ts 存在且包含 describe('Quotes data')
    - pnpm test 运行无配置错误
  </acceptance_criteria>
  <done>所有测试文件骨架创建完成，测试框架可运行</done>
</task>

</tasks>

<verification>
- pnpm test 命令可执行
- 所有测试文件存在
- 测试运行无配置错误
</verification>

<success_criteria>

- Vitest 安装成功
- vitest.config.ts 配置正确
- 5 个测试文件骨架创建完成
- pnpm test 运行正常 </success_criteria>

<output>
完成后创建 `.planning/phases/01-article-management/01-01-SUMMARY.md`
</output>
