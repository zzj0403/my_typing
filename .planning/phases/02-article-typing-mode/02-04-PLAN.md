---
phase: 02-article-typing-mode
plan: 04
type: execute
wave: 3
depends_on: [02-03]
files_modified: [src/pages/Hero/index.tsx]
autonomous: true
requirements: [TYP-01, TYP-06, UI-01, UI-05]

must_haves:
  truths:
    - "Hero page shows TypingGame component for selected article"
    - "User can select article from ArticleList Drawer and start typing"
    - "Drawer closes after article selection"
  artifacts:
    - path: "src/pages/Hero/index.tsx"
      provides: "Main Hero page with TypingGame integration"
      contains: "TypingGame"
  key_links:
    - from: "src/pages/Hero/index.tsx"
      to: "src/components/TypingGame/index.tsx"
      via: "TypingGame import"
      pattern: "import.*TypingGame"
    - from: "src/pages/Hero/index.tsx"
      to: "src/components/ArticleList/index.tsx"
      via: "ArticleList in Drawer"
      pattern: "ArticleList"
---

<objective>
Integrate TypingGame component into Hero page, replacing the existing single-character Hanzi practice mode.

Purpose: Enable full article typing experience in the main page
Output: Modified Hero page with TypingGame component
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-article-typing-mode/02-RESEARCH.md
@.planning/phases/02-article-typing-mode/02-UI-SPEC.md

<interfaces>
<!-- Key types the executor needs. Extracted from codebase. -->

From src/pages/Hero/index.tsx (current implementation):
```typescript
// Current imports
import { Hanzi } from '@/components'
import ArticleList from '@/components/ArticleList'
import { useArticleStore } from '@/stores'

// Current state
const [articleListVisible, { toggle: toggleArticleListVisible }] = useBoolean(false)
const { currentArticleId, selectArticle, getArticleById } = useArticleStore()

// Current article selection handler
const handleArticleSelect = React.useCallback((articleId: string) => {
  selectArticle(articleId)
  // ... TextRegister integration code
  toggleArticleListVisible()
}, [selectArticle, getArticleById, onChangeBin, toggleArticleListVisible])

// Current render
return (
  <div className={styles.app}>
    <div>
      <Hanzi ... />  // This should be replaced with TypingGame
    </div>
    {/* ... menu and drawers */}
  </div>
)
```

From src/components/TypingGame/index.tsx (created in 02-03):
```typescript
export function TypingGame() {
  // Uses useArticleStore internally
  // Renders article display, progress, and input controls
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Hanzi component with TypingGame in Hero page</name>
  <files>src/pages/Hero/index.tsx</files>
  <read_first>
    - src/pages/Hero/index.tsx (current implementation)
    - src/components/TypingGame/index.tsx (TypingGame component)
    - src/stores/articleStore.ts (store usage)
  </read_first>
  <acceptance_criteria>
    - TypingGame component is imported from '@/components'
    - Hanzi component is removed from the main content area
    - TypingGame is rendered in the main content area
    - Article selection from Drawer triggers TypingGame to show selected article
    - Existing menu buttons and Drawer functionality remain unchanged
    - Select component for text templates is removed (no longer needed)
  </acceptance_criteria>
  <action>Modify Hero page to integrate TypingGame:

1. Update imports - add TypingGame, remove unused Hanzi-related imports:
```typescript
import {
  Button,
  Drawer,
  Form,
  Input,
  List,
  Modal,
  Tooltip,
  Typography,
} from 'antd'
import {
  CloudDownloadOutlined,
  CloudUploadOutlined,
  GithubOutlined,
  LoadingOutlined,
  RedoOutlined,
  SettingOutlined,
  FileTextOutlined,
} from '@ant-design/icons'
import { useBoolean } from 'ahooks'

import useProfileBin from './useProfileBin'
import styles from './index.module.less'
import { Registry } from '@/core'
import { TypingGame } from '@/components'  // Add this
import ArticleList from '@/components/ArticleList'
import { useArticleStore } from '@/stores'
```

2. Remove the textOptions, currentCharConfig, currentPinyin logic and the useEffect for matching - these are now handled by useTypingGame

3. Simplify handleArticleSelect - just select and close drawer:
```typescript
const handleArticleSelect = React.useCallback((articleId: string) => {
  selectArticle(articleId)
  toggleArticleListVisible()
}, [selectArticle, toggleArticleListVisible])
```

4. Replace the main content area:
```typescript
return (
  <div className={styles.app}>
    <div className={styles.mainContent}>
      <TypingGame />
    </div>
    <div className={styles.menu}>
      {/* Keep existing menu buttons but remove Select */}
      <Input.Group compact>
        <Button
          icon={<FileTextOutlined />}
          onClick={() => toggleArticleListVisible()}
        />
        <Button
          icon={<SettingOutlined />}
          onClick={() => toggleSettingsVisible()}
        />
        {/* Remove the Select component for text templates */}
        <Tooltip overlay='重置本地输入状态' mouseEnterDelay={mouseEnterDelay}>
          <Button
            onClick={() => {
              // Reset now handled by TypingGame component
            }}
            icon={<RedoOutlined />}
          />
        </Tooltip>
        {/* Keep cloud sync buttons */}
        {/* ... */}
      </Input.Group>
    </div>
    {/* Keep settings Drawer */}
    {/* Keep article list Drawer */}
  </div>
)
```

5. Add CSS for mainContent if needed in index.module.less:
```less
.mainContent {
  flex: 1;
  display: flex;
  flex-direction: column;
}
```</action>
  <verify>
    <automated>grep -E "TypingGame" src/pages/Hero/index.tsx</automated>
  </verify>
  <done>Hero page renders TypingGame component, article selection works, Drawer closes on selection</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full article typing mode integration in Hero page - TypingGame component replaces single-character Hanzi practice</what-built>
  <how-to-verify>
    1. Run `npm run dev` to start development server
    2. Open http://localhost:5173 in browser
    3. Click the file icon button (top left) to open ArticleList Drawer
    4. Select any article from the list (e.g., a poem or quote)
    5. Verify: Drawer closes and article content appears in main area
    6. Type pinyin for the first character - verify it turns green on correct input
    7. Press ESC to skip a character - verify it turns gray
    8. Continue typing until completion - verify success message appears
    9. Test punctuation input - type ',' for Chinese comma
  </how-to-verify>
  <resume-signal>Type "approved" if typing mode works correctly, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- TypingGame component renders in Hero page
- Article selection from Drawer triggers content display
- All typing functionality works (pinyin matching, punctuation, skip, completion)
- Existing settings Drawer still works
- Cloud sync buttons remain functional
</verification>

<success_criteria>
- Hero page displays TypingGame component instead of Hanzi
- Article selection from ArticleList works correctly
- All TYP-01~06 and UI-01~05 requirements are met
- User can complete full article typing practice
</success_criteria>

<output>
After completion, create `.planning/phases/02-article-typing-mode/02-04-SUMMARY.md`
</output>
