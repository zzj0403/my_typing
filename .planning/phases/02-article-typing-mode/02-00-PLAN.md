---
phase: 02-article-typing-mode
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: [tests/hooks/useTypingGame.test.ts, tests/components/CharSpan.test.tsx, tests/components/TypingGame.test.tsx]
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "All Phase 2 tests have stub files ready for implementation"
    - "Test files can be run by vitest without errors"
  artifacts:
    - path: "tests/hooks/useTypingGame.test.ts"
      provides: "Test stubs for TYP-02, TYP-03, TYP-04, TYP-05, UI-05"
      min_lines: 20
    - path: "tests/components/CharSpan.test.tsx"
      provides: "Test stubs for UI-02, UI-03, UI-04"
      min_lines: 15
    - path: "tests/components/TypingGame.test.tsx"
      provides: "Test stubs for TYP-01, TYP-06"
      min_lines: 15
  key_links: []
---

<objective>
Create test infrastructure stubs for Phase 2 typing mode implementation.

Purpose: Provide Nyquist-compliant test scaffolds for TDD workflow
Output: Three test files with describe blocks and pending test cases
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-article-typing-mode/02-VALIDATION.md
@.planning/phases/02-article-typing-mode/02-RESEARCH.md

<interfaces>
<!-- Existing test patterns from Phase 1 -->
From tests/stores/articleStore.test.ts:
- Uses vitest describe/it/expect pattern
- Mocks Zustand store with act() wrapper
- Tests CRUD operations with state assertions

From tests/utils/sentenceSplit.test.ts:
- Pure function testing pattern
- Multiple test cases with it.each()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useTypingGame hook test stubs</name>
  <files>tests/hooks/useTypingGame.test.ts</files>
  <read_first>
    - tests/stores/articleStore.test.ts (existing test patterns)
    - vitest.config.ts (test configuration)
  </read_first>
  <behavior>
    - Test: handleInput correctly matches pinyin for Hanzi characters
    - Test: handleInput marks character as correct when pinyin matches
    - Test: handleInput marks character as incorrect when pinyin does not match
    - Test: skipCurrent marks current character as skipped and moves to next
    - Test: punctuation matching supports English-to-Chinese mapping
    - Test: progress calculation returns correct typed/total/counts
  </behavior>
  <action>Create test file with describe block 'useTypingGame' containing 6 it.todo() stubs:
1. 'should match pinyin for Hanzi characters (TYP-02)'
2. 'should mark character as correct when pinyin matches (TYP-03)'
3. 'should mark character as incorrect and continue (TYP-04)'
4. 'should skip current character on ESC (TYP-04)'
5. 'should match punctuation with English-to-Chinese mapping (TYP-05)'
6. 'should calculate progress correctly (UI-05)'

Add imports for describe, it, expect, vi from vitest.
Add placeholder for mock article data structure.</action>
  <verify>
    <automated>npm run test -- tests/hooks/useTypingGame.test.ts 2>&1 | grep -E "(PASS|pending|TODO)"</automated>
  </verify>
  <done>Test file exists with 6 pending test cases, vitest runs without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create CharSpan component test stubs</name>
  <files>tests/components/CharSpan.test.tsx</files>
  <read_first>
    - tests/stores/articleStore.test.ts (existing test patterns)
    - vitest.config.ts (test configuration)
    - src/components/ArticleList/index.tsx (component testing reference)
  </read_first>
  <behavior>
    - Test: renders character with current state (blue underline)
    - Test: renders character with correct state (green color)
    - Test: renders character with incorrect state (red + strikethrough)
  </behavior>
  <action>Create test file with describe block 'CharSpan' containing 3 it.todo() stubs:
1. 'should highlight current input position (UI-02)'
2. 'should apply green color for correct characters (UI-03)'
3. 'should apply red color and strikethrough for incorrect characters (UI-04)'

Add imports for describe, it, expect from vitest.
Add @testing-library/react imports placeholder.</action>
  <verify>
    <automated>npm run test -- tests/components/CharSpan.test.tsx 2>&1 | grep -E "(PASS|pending|TODO)"</automated>
  </verify>
  <done>Test file exists with 3 pending test cases, vitest runs without errors</done>
</task>

<task type="auto">
  <name>Task 3: Create TypingGame component test stubs</name>
  <files>tests/components/TypingGame.test.tsx</files>
  <read_first>
    - tests/stores/articleStore.test.ts (existing test patterns)
    - vitest.config.ts (test configuration)
  </read_first>
  <behavior>
    - Test: displays article content for typing (original text visible)
    - Test: shows completion message when all characters typed
  </behavior>
  <action>Create test file with describe block 'TypingGame' containing 2 it.todo() stubs:
1. 'should display article content for sentence-by-sentence typing (TYP-01)'
2. 'should show completion message when article is finished (TYP-06)'

Add imports for describe, it, expect from vitest.
Add @testing-library/react imports placeholder.</action>
  <verify>
    <automated>npm run test -- tests/components/TypingGame.test.tsx 2>&1 | grep -E "(PASS|pending|TODO)"</automated>
  </verify>
  <done>Test file exists with 2 pending test cases, vitest runs without errors</done>
</task>

</tasks>

<verification>
- All 3 test files exist and are syntactically valid
- npm run test runs all stubs without errors
- Each test file has describe block with it.todo() stubs
</verification>

<success_criteria>
- tests/hooks/useTypingGame.test.ts has 6 pending test cases
- tests/components/CharSpan.test.tsx has 3 pending test cases
- tests/components/TypingGame.test.tsx has 2 pending test cases
- All tests can be run with npm run test
</success_criteria>

<output>
After completion, create `.planning/phases/02-article-typing-mode/02-00-SUMMARY.md`
</output>
