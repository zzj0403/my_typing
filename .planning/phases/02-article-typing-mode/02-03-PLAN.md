---
phase: 02-article-typing-mode
plan: 03
type: tdd
wave: 2
depends_on: [02-01, 02-02]
files_modified: [src/components/TypingGame/ArticleDisplay.tsx, src/components/TypingGame/index.tsx, src/components/TypingGame/index.module.less, src/hooks/index.ts, src/components/index.ts, tests/components/TypingGame.test.tsx]
autonomous: true
requirements: [TYP-01, TYP-06, UI-01]

must_haves:
  truths:
    - "Article content is displayed in ArticleDisplay component with sentence grouping"
    - "Current character has blue underline highlight"
    - "Progress bar updates in real-time"
    - "Completion message shows when all characters are typed"
  artifacts:
    - path: "src/components/TypingGame/ArticleDisplay.tsx"
      provides: "Article content display with sentence grouping"
      exports: ["ArticleDisplay"]
    - path: "src/components/TypingGame/index.tsx"
      provides: "Main typing game container with input handling"
      exports: ["TypingGame"]
    - path: "src/components/TypingGame/index.module.less"
      provides: "TypingGame layout styles"
      contains: ".container, .header, .inputArea"
  key_links:
    - from: "src/components/TypingGame/index.tsx"
      to: "src/hooks/useTypingGame.ts"
      via: "useTypingGame hook import"
      pattern: "import.*useTypingGame"
    - from: "src/components/TypingGame/index.tsx"
      to: "src/stores/articleStore.ts"
      via: "useArticleStore for currentArticleId"
      pattern: "useArticleStore"
    - from: "src/components/TypingGame/ArticleDisplay.tsx"
      to: "src/components/TypingGame/CharSpan.tsx"
      via: "CharSpan import for rendering"
      pattern: "import.*CharSpan"
---

<objective>
Create ArticleDisplay component and main TypingGame component with full input handling.

Purpose: Display article content and handle user typing input
Output: ArticleDisplay component, TypingGame main component with progress and completion
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-article-typing-mode/02-RESEARCH.md
@.planning/phases/02-article-typing-mode/02-UI-SPEC.md

<interfaces>
<!-- Key types the executor needs. Extracted from codebase. -->

From src/types/typing.ts (created in 02-01):
```typescript
export enum CharState {
  Pending = 'pending',
  Correct = 'correct',
  Incorrect = 'incorrect',
  Skipped = 'skipped',
}

export interface TypingChar extends CharInfo {
  state: CharState
  inputPinyin?: string
}

export interface TypingProgress {
  typed: number
  total: number
  correct: number
  incorrect: number
  skipped: number
}
```

From src/types/article.ts:
```typescript
export interface Article {
  id: string
  key: string
  title: string
  description?: string
  content: string
  sentences: Sentence[]
  source: ArticleSource
  createdAt: number
}

export interface Sentence {
  id: string
  text: string
  chars: CharInfo[]
}
```

From src/hooks/useTypingGame.ts (created in 02-01):
```typescript
export function useTypingGame(article: Article | null) {
  return {
    chars: TypingChar[]
    currentIndex: number
    progress: TypingProgress
    isComplete: boolean
    handleInput: (value: string) => void
    skipCurrent: () => void
    reset: () => void
  }
}
```

From src/stores/articleStore.ts:
```typescript
export const useArticleStore = create<ArticleState>()
// Returns: articles, currentArticleId, selectArticle, getArticleById
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TypingGame styles</name>
  <files>src/components/TypingGame/index.module.less</files>
  <read_first>
    - src/components/ArticleList/index.module.less (existing style patterns)
    - .planning/phases/02-article-typing-mode/02-UI-SPEC.md (layout specifications)
  </read_first>
  <acceptance_criteria>
    - File contains .container class with proper padding
    - File contains .header class for title and progress area
    - File contains .articleDisplay class for content area
    - File contains .inputArea class for input controls
    - File contains .input class for text input styling
    - File contains .buttons class for action buttons
  </acceptance_criteria>
  <action>Create Less module with layout styles following UI-SPEC.md:

```less
.container {
  display: flex;
  flex-direction: column;
  height: 100%;
  padding: 24px;
}

.header {
  margin-bottom: 24px;
}

.title {
  margin-bottom: 16px;
}

.progressWrapper {
  display: flex;
  align-items: center;
  gap: 16px;
}

.progressBar {
  flex: 1;
}

.progressText {
  font-size: 14px;
  color: #8c8c8c;
  white-space: nowrap;
}

.articleDisplay {
  flex: 1;
  overflow-y: auto;
  padding: 24px;
  background: #ffffff;
  border-radius: 8px;
  margin-bottom: 24px;
  font-size: 24px;
  line-height: 2;
}

.sentence {
  margin-bottom: 16px;
  &:last-child {
    margin-bottom: 0;
  }
}

.inputArea {
  display: flex;
  gap: 12px;
  align-items: center;
}

.input {
  flex: 1;
  height: 44px;
  padding: 8px 16px;
  font-size: 16px;
  border: 1px solid #d9d9d9;
  border-radius: 4px;
  outline: none;
  transition: border-color 0.2s;

  &:focus {
    border-color: #1890ff;
  }

  &::placeholder {
    color: #bfbfbf;
  }
}

.buttons {
  display: flex;
  gap: 8px;
}
```</action>
  <verify>
    <automated>grep -E "(\.container|\.header|\.articleDisplay|\.inputArea|\.input)" src/components/TypingGame/index.module.less</automated>
  </verify>
  <done>Style file exists with container, header, articleDisplay, inputArea, input classes</done>
</task>

<task type="auto">
  <name>Task 2: Create ArticleDisplay component</name>
  <files>src/components/TypingGame/ArticleDisplay.tsx</files>
  <read_first>
    - src/types/typing.ts (TypingChar, CharState)
    - src/types/article.ts (Sentence, CharInfo)
    - src/components/TypingGame/CharSpan.tsx (CharSpan component)
    - src/components/TypingGame/index.module.less (styles)
  </read_first>
  <acceptance_criteria>
    - File exports ArticleDisplay React component
    - Component accepts sentences, chars, currentIndex props
    - Component renders CharSpan for each character
    - Current character is identified by index comparison
    - Characters are grouped by sentence
  </acceptance_criteria>
  <action>Create ArticleDisplay component:

```typescript
import React from 'react'
import type { Sentence, CharInfo } from '@/types/article'
import { CharSpan } from './CharSpan'
import type { TypingChar } from '@/types/typing'
import styles from './index.module.less'

export interface ArticleDisplayProps {
  sentences: Sentence[]
  chars: TypingChar[]
  currentIndex: number
}

export function ArticleDisplay({ sentences, chars, currentIndex }: ArticleDisplayProps) {
  // Calculate character offset for each sentence
  let charOffset = 0

  return (
    <div className={styles.articleDisplay}>
      {sentences.map((sentence, sentenceIndex) => {
        const sentenceStart = charOffset
        const sentenceEnd = charOffset + sentence.chars.length
        charOffset = sentenceEnd

        return (
          <div key={sentence.id} className={styles.sentence}>
            {sentence.chars.map((char, charIndex) => {
              const globalIndex = sentenceStart + charIndex
              const typingChar = chars[globalIndex]

              return (
                <CharSpan
                  key={`${sentence.id}-${charIndex}`}
                  char={char}
                  state={typingChar?.state || 'pending'}
                  isCurrent={globalIndex === currentIndex}
                  inputPinyin={typingChar?.inputPinyin}
                />
              )
            })}
          </div>
        )
      })}
    </div>
  )
}
```</action>
  <verify>
    <automated>grep -E "export function ArticleDisplay" src/components/TypingGame/ArticleDisplay.tsx</automated>
  </verify>
  <done>ArticleDisplay component exists and exports correctly</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create TypingGame main component with tests</name>
  <files>src/components/TypingGame/index.tsx, tests/components/TypingGame.test.tsx, src/hooks/index.ts, src/components/index.ts</files>
  <read_first>
    - src/hooks/useTypingGame.ts (hook implementation)
    - src/stores/articleStore.ts (store usage pattern)
    - src/types/typing.ts (TypingProgress)
    - src/types/article.ts (Article)
    - src/components/TypingGame/ArticleDisplay.tsx (ArticleDisplay)
    - src/components/TypingGame/index.module.less (styles)
  </read_first>
  <behavior>
    - Test: displays article content for sentence-by-sentence typing
    - Test: shows completion message when article is finished
  </behavior>
  <action>Implement TypingGame component following TDD:

**RED Phase:** Update tests/components/TypingGame.test.tsx:
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen, fireEvent, waitFor } from '@testing-library/react'
import { TypingGame } from '@/components/TypingGame'
import { useArticleStore } from '@/stores/articleStore'
import { CharType } from '@/core'

// Mock the store
vi.mock('@/stores/articleStore', () => ({
  useArticleStore: vi.fn(),
}))

const mockArticle = {
  id: 'test-1',
  key: 'test',
  title: 'Test Article',
  content: '你好。',
  sentences: [
    {
      id: 's1',
      text: '你好。',
      chars: [
        { type: CharType.Hanzi, char: '你', quanpin: 'ni' },
        { type: CharType.Hanzi, char: '好', quanpin: 'hao' },
        { type: CharType.Mark, char: '。' },
      ],
    },
  ],
  source: 'builtin' as const,
  createdAt: Date.now(),
}

describe('TypingGame', () => {
  beforeEach(() => {
    vi.clearAllMocks()
  })

  it('should display article content for typing (TYP-01)', () => {
    ;(useArticleStore as any).mockReturnValue({
      currentArticleId: 'test-1',
      getArticleById: () => mockArticle,
    })

    render(<TypingGame />)

    expect(screen.getByText('Test Article')).toBeInTheDocument()
    expect(screen.getByText('你')).toBeInTheDocument()
    expect(screen.getByText('好')).toBeInTheDocument()
    expect(screen.getByText('。')).toBeInTheDocument()
  })

  it('should show completion message when article is finished (TYP-06)', async () => {
    ;(useArticleStore as any).mockReturnValue({
      currentArticleId: 'test-1',
      getArticleById: () => mockArticle,
    })

    render(<TypingGame />)

    // Type all characters
    const input = screen.getByPlaceholderText('输入拼音...')

    // Type 'ni' for first char
    fireEvent.change(input, { target: { value: 'ni' } })
    fireEvent.compositionEnd(input, { currentTarget: { value: 'ni' } })

    // Type 'hao' for second char
    fireEvent.change(input, { target: { value: 'hao' } })
    fireEvent.compositionEnd(input, { currentTarget: { value: 'hao' } })

    // Type '.' for punctuation
    fireEvent.keyDown(input, { key: '.' })

    await waitFor(() => {
      expect(screen.getByText(/恭喜/)).toBeInTheDocument()
    })
  })
})
```

**GREEN Phase:** Create src/components/TypingGame/index.tsx:
```typescript
import React, { useState, useCallback, useEffect } from 'react'
import { Button, Progress, Typography, message } from 'antd'
import { useArticleStore } from '@/stores/articleStore'
import { useTypingGame } from '@/hooks/useTypingGame'
import { CharType } from '@/core'
import { ArticleDisplay } from './ArticleDisplay'
import styles from './index.module.less'

const { Title } = Typography

export function TypingGame() {
  const { currentArticleId, getArticleById } = useArticleStore()
  const article = currentArticleId ? getArticleById(currentArticleId) : null

  const {
    chars,
    currentIndex,
    progress,
    isComplete,
    handleInput,
    skipCurrent,
    reset,
  } = useTypingGame(article)

  const [isComposing, setIsComposing] = useState(false)
  const [inputBuffer, setInputBuffer] = useState('')

  // Completion message
  useEffect(() => {
    if (isComplete) {
      message.success('恭喜！文章打字完成！')
    }
  }, [isComplete])

  const handleCompositionStart = useCallback(() => {
    setIsComposing(true)
  }, [])

  const handleCompositionEnd = useCallback((e: React.CompositionEvent<HTMLInputElement>) => {
    setIsComposing(false)
    const value = e.currentTarget.value
    handleInput(value)
    setInputBuffer('')
  }, [handleInput])

  const handleInputChange = useCallback((e: React.ChangeEvent<HTMLInputElement>) => {
    const value = e.target.value
    setInputBuffer(value)

    // Handle punctuation directly when not composing
    if (!isComposing) {
      const currentChar = chars[currentIndex]
      if (currentChar?.type === CharType.Mark && value === currentChar.char) {
        handleInput(value)
        setInputBuffer('')
      }
    }
  }, [isComposing, chars, currentIndex, handleInput])

  const handleKeyDown = useCallback((e: React.KeyboardEvent<HTMLInputElement>) => {
    if (e.key === 'Escape') {
      skipCurrent()
      setInputBuffer('')
      return
    }
  }, [skipCurrent])

  const handleSkip = useCallback(() => {
    skipCurrent()
    setInputBuffer('')
  }, [skipCurrent])

  const handleReset = useCallback(() => {
    reset()
    setInputBuffer('')
  }, [reset])

  if (!article) {
    return (
      <div className={styles.container}>
        <div className={styles.articleDisplay}>
          <Typography.Text type="secondary">请先选择一篇文章</Typography.Text>
        </div>
      </div>
    )
  }

  const currentChar = chars[currentIndex]
  const placeholder = currentChar?.type === CharType.Hanzi ? '输入拼音...' : '输入标点...'

  return (
    <div className={styles.container}>
      <div className={styles.header}>
        <Title level={4} className={styles.title}>{article.title}</Title>
        <div className={styles.progressWrapper}>
          <Progress
            className={styles.progressBar}
            percent={Math.round((progress.typed / progress.total) * 100)}
            status={isComplete ? 'success' : 'active'}
          />
          <span className={styles.progressText}>
            {progress.typed} / {progress.total} 字
          </span>
        </div>
      </div>

      <ArticleDisplay
        sentences={article.sentences}
        chars={chars}
        currentIndex={currentIndex}
      />

      <div className={styles.inputArea}>
        <input
          className={styles.input}
          value={inputBuffer}
          onChange={handleInputChange}
          onCompositionStart={handleCompositionStart}
          onCompositionEnd={handleCompositionEnd}
          onKeyDown={handleKeyDown}
          placeholder={placeholder}
          autoFocus
        />
        <div className={styles.buttons}>
          <Button onClick={handleSkip}>跳过 (ESC)</Button>
          <Button onClick={handleReset}>重置</Button>
        </div>
      </div>
    </div>
  )
}
```

**Update exports:**
- Add to src/hooks/index.ts: `export { useTypingGame } from './useTypingGame'`
- Add to src/components/index.ts: `export { TypingGame } from './TypingGame'`

Run tests: npm run test -- tests/components/TypingGame.test.tsx</action>
  <verify>
    <automated>npm run test -- tests/components/TypingGame.test.tsx 2>&1 | grep -E "(PASS|2 passed)"</automated>
  </verify>
  <done>All 2 tests pass, TypingGame component displays article with progress and completion message</done>
</task>

</tasks>

<verification>
- TypingGame shows article title and progress bar
- ArticleDisplay renders all sentences and characters
- Input accepts pinyin through composition events
- ESC triggers skip functionality
- Completion message shows when isComplete is true
</verification>

<success_criteria>
- src/components/TypingGame/index.tsx exports TypingGame component
- src/components/TypingGame/ArticleDisplay.tsx exports ArticleDisplay component
- All 2 integration tests pass
- Component handles null article gracefully with placeholder text
- Progress bar updates based on progress.typed/total
</success_criteria>

<output>
After completion, create `.planning/phases/02-article-typing-mode/02-03-SUMMARY.md`
</output>
